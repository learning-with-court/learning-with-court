{
  "name": "learning-with-court-plugin",
  "version": "0.0.0",
  "private": true,
  "description": "Tooling shell for the Claude Code plugin + marketplace. Holds lefthook + validation scripts; the plugins themselves ship from plugins/.",
  "scripts": {
    "validate": "node scripts/validate-plugin.mjs",
    "postinstall": "lefthook install"
  },
  "devDependencies": {
    "lefthook": "^2.1.6"
  }
}
